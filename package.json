{
  "name": "kata-parcel-test",
  "version": "0.1.1",
  "description": "Trying out the parcel bundler",
  "main": "dist/app.js",
  "scripts": {
    "build": "parcel build index.html --out-dir=prod --public-url ./ && http-server -p 8081 ./prod",
    "start": "parcel index.html",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Martin Feineis",
  "license": "MIT",
  "devDependencies": {
    "babel-core": "^6.26.3",
    "babel-plugin-transform-object-rest-spread": "^6.26.0",
    "babel-preset-env": "^1.7.0",
    "http-server": "^0.11.1",
    "less": "^3.9.0",
    "parcel-bundler": "^1.12.3",
    "typescript": "^3.5.2"
  },
  "dependencies": {
    "ramda": "^0.26.1"
  }
}
